<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Job_Title %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Employer %><br>
    <%= f.text_field :employer %>
  </div>
  <div class="field">
    <%= f.label :Sector %><br>
	<%= f.collection_select :sector_id, Sector.all, :id, :sector_name %>
  </div>
  <div class="field">
    <%= f.label :Experience_Required %><br>
    <%= f.text_area :experience_req %>
  </div>
  <div class="field">
    <%= f.label :Job_Information %><br>
    <%= f.text_area :job_info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
